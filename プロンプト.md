# 依頼事項
Railsプロジェクトを新規作成し、新人がスムーズに開発に参加できる環境（Spec-Kit準拠）を構築します。
以下の構成で、セットアップ手順を含むREADME.mdと、環境構築用のシェルスクリプトを作成してください。

# 技術構成
- Ruby 3.x / Rails 7.x
- DB: PostgreSQL (Docker Composeを利用)
- 認証: Devise
- 管理画面: ActiveAdmin
- Lint/Format: RuboCop, Prettier
- Test: RSpec, FactoryBot

# 出力項目
1. GitHubの標準的な構成に従った README.md (Usage, Setup, Testing, Deployment)
2. `bin/setup` スクリプト（DB作成、seed投入、ライブラリインストールを自動化するもの）
3. Docker Compose構成ファイル (postgres, redis)
4. .github/workflows/ci.yml (GitHub ActionsでRSpecとRuboCopを実行する設定)

# Project Name

[![CI](https://github.com/org/repo/actions/workflows/ci.yml/badge.svg)](https://github.com/org/repo/actions)

## 📋 概要
AIを活用した[アプリ名]のプロジェクトです。

## 🛠 開発環境のセットアップ

### 前提条件
- Ruby 3.x
- Node.js / Yarn
- Docker / Docker Compose

### 構築手順
1. **リポジトリのクローン**
   ```bash
   git clone git@github.com:org/repo.git
   cd repo
   ```

## 3. Spec-Kit 対応のポイント

新人が「何を見ればいいかわからない」状態を防ぐため、以下の3点をRailsプロジェクト内に含めるのがコツです。

### ① `bin/setup` の活用
Rails標準の `bin/setup` をカスタマイズし、`bundle install` や `yarn install`、`db:prepare` を1コマンドで終わるようにしておきます。

### ② Docker Compose の同梱
ローカルPCにPostgreSQLを直接インストールさせると、バージョン差異でハマる原因になります。DB周りはDockerで完結させるのが現場のスタンダードです。

### ③ `.env.example` の徹底
APIキー（OpenAIなど）が必要な場合、空の値を設定した `.env.example` を必ずGit管理に含め、新人が何を準備すべきか可視化します。

---

# 依頼事項
Ruby on Rails (7.x) で以下の画面を実装してください。
Railsの規約（Convention over Configuration）を重視し、保守性の高いコードを生成してください。

# 実装対象の画面
[例：ユーザーがAIとチャットする履歴一覧画面と詳細画面]

# 技術制約
- CSS Framework: Tailwind CSS
- Interaction: Hotwire (Turbo/Stimulus) を使用し、ページ遷移なしでリッチに動かしたい
- Icons: Heroicons (lucide-rails)
- Component: ViewComponent を使用（もし導入済みであれば）

# デザイン・UI要件
- 全体的に「[例：シンプルかつモダン、Apple製品のような清潔感]」なデザインにしてください。
- 認証済みのユーザー（Devise: current_user）のみがアクセス可能です。
- レスポンシブ対応（スマホ・PC両方）を必須とします。

# 出力項目
1. 該当する Controller のアクションコード
2. View ファイル (index.html.erb, show.html.erb 等)
3. 必要であれば Stimulus Controller (JS)
4. ルーティング設定 (config/routes.rb)